<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro Empresa</title>
    <script type="text/javascript" src="../../script/index.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }

        /* Estilo do fundo escuro */
        .modal-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5); /* Cor escura com opacidade */
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1;
        }

        /* Estilo do modal */
        .modal {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            width: 60%;
            max-width: 400px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            max-height: 80vh; /* Defina a altura máxima do modal */
            overflow-y: auto;
        }

        /* Estilo do botão de fechar */
        .close-btn {
            cursor: pointer;
            position: absolute;
            top: 10px;
            right: 10px;
        }

        /* Estilo do formulário */
        form {
            display: flex;
            flex-direction: column;
            max-height: 100%; /* Garanta que o formulário não ultrapasse a altura do modal */
            overflow-y: auto;
        }

        /* Estilo dos campos do formulário */
        label {
            margin-bottom: 5px;
        }

        input {
            margin-bottom: 15px;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        input[type="submit"] {
            cursor: pointer;
            background-color: #4caf50;
            color: #fff;
            border: none;
        }
    </style>
</head>
<body>

    <!-- Modal -->
    <div class="modal-background" id="modalBackground">
        
        <div class="modal">
            <!-- Botão de fechar movido para dentro do modal -->
            <span class="close-btn" onclick="closeModal()">&times;</span>
            <!-- Formulário com os campos específicos -->
            <form method="post" action="/cadastra/empresa">
                <label for="name">Nome:</label>
                <input type="text" required="required" id="name" name="name">

                <label for="password">Senha:</label>
                <input type="password" required="required" id="password" name="password">

                <label for="cnpj">Cnpj:</label>
                <input type="number" required="required" id="cnpj" name="cnpj">

                <label for="email">Email:</label>
                <input type="number" required="required" id="email" name="email">

                <input type="submit" value="Enviar">
            </form>
        </div>

    </div>

</body>
</html>
