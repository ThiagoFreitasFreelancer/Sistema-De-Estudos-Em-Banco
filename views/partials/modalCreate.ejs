<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modal Formulário</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        /* Estilo do fundo escuro */
        .modal-background {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5); /* Cor escura com opacidade */
            justify-content: center;
            align-items: center;
            z-index: 1;
        }

        /* Estilo do modal */
        .modal {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            width: 60%;
            max-width: 400px;
            margin: 0 auto;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Estilo do botão de fechar */
        .close-btn {
            cursor: pointer;
            position: absolute;
            top: 10px;
            right: 10px;
        }
    </style>
</head>
<body>

<!-- Modal -->
<div class="modal-background" id="modalBackground">
    <div class="modal">
        <span class="close-btn" onclick="closeModal()">&times;</span>
        <!-- Aqui você pode adicionar os campos do formulário -->
        <form>
            <label for="id">ID:</label>
            <input type="text" id="id" name="id">
            
            <!-- Adicione outros campos aqui... -->

            <input type="submit" value="Enviar">
        </form>
    </div>
</div>

</body>
</html>
